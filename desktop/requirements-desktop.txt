# Ribbon Tracker — desktop build requirements
# ============================================
# Install order matters. The CI workflow re-runs the setuptools pin LAST so it
# wins against transitive bumps; see build-windows.yml.

# 1. The setuptools pin — MUST be exactly 65.5.1. Newer setuptools makes
#    pkg_resources strict and crashes the frozen exe at launch. Co-pinned with
#    Python 3.11 (see RibbonTracker.spec comment block).
setuptools==65.5.1

# 2. pkg_resources' vendored packages, installed as REAL top-level packages so
#    its extern-importer has a working fallback at runtime. Without these the
#    frozen exe dies with "the jaraco package is required".
jaraco.text
jaraco.functools
jaraco.context
more-itertools
packaging
platformdirs
appdirs
ordered-set

# 3. App runtime — same versions the dev app uses.
streamlit>=1.50.0
pandas>=2.0.0
altair>=5.0.0
numpy>=1.26.0
openpyxl>=3.1.0

# 4. Build tool
pyinstaller>=6.0.0

# 5. Test runner — for the CI boot self-test step.
pytest>=7.0.0
